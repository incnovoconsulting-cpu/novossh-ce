import React from 'react';
import ReactDOM from 'react-dom/client';
import { TauriApp } from './components/TauriApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TauriApp />
  </React.StrictMode>
);
